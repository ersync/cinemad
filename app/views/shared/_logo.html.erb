<div class="inline-block select-none relative group animate-pulse-glow">
  <%= image_tag "logo.png",
    class: "inline-block w-auto
           [filter:brightness(0)_invert(1)_sepia(1)_saturate(2000%)_hue-rotate(170deg)]
           transition-all duration-300
           group-hover:[filter:brightness(0)_invert(1)_sepia(1)_saturate(2500%)_hue-rotate(170deg)_drop-shadow(0_0_8px_#4FB5FF)]",
    alt: "CinEmad Logo"
  %>
</div>

<style>
    @keyframes pulse-glow {
        0%, 100% {
            filter: brightness(0) invert(1) sepia(1) saturate(2000%) hue-rotate(170deg);
        }
        50% {
            filter: brightness(0) invert(1) sepia(1) saturate(2500%) hue-rotate(170deg) drop-shadow(0 0 8px #4FB5FF);
        }
    }

    .animate-pulse-glow img {
        animation: pulse-glow 4s ease-in-out infinite;
    }
</style>